apiVersion: v1
kind: Pod
metadata:
  name: hostpath-pod
spec:
  containers:
  - name: test-container
    image: busybox
    command: [ "sleep", "3600" ]   # Keep the container running
    volumeMounts:
    - name: host-volume
      mountPath: /mnt/hostdir
  volumes:
  - name: host-volume
    hostPath:
      path: /data/hostpath-demo     # Path on the host
      type: Directory

azureuser@Re-K8S-machine:~$ cat cicd-token.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cicd-token
  namespace: app-team1
